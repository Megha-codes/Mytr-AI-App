# Test/dev-only dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
# Async SQLite driver — the device store runs on SQLite for the offline
# end-to-end proof and the test suite.
aiosqlite>=0.19.0
# In-process ASGI transport for exercising the FastAPI routes in tests.
asgi-lifespan>=2.1.0
