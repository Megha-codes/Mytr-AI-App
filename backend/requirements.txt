annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
asyncpg==0.31.0
click==8.3.3
colorama==0.4.6
fastapi==0.136.1
greenlet==3.5.0
h11==0.16.0
idna==3.13
pydantic==2.13.3
pydantic_core==2.46.3
SQLAlchemy==2.0.49
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
# HTTP client for the LibreLinkUp poller (also used by the CGM services).
httpx>=0.27.0
# WebSocket server support for the /v1/devices/stream endpoint.
websockets>=12.0
# Loads backend/.env (LIBRE_* / JWT_SECRET / DB URLs) in local/dev runs.
python-dotenv>=1.0.0
# IANA timezone data for zoneinfo (Windows has no system tz db) — used to
# convert the Libre account's naive timestamps to UTC.
tzdata>=2024.1
