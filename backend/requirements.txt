annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
asyncpg==0.31.0
click==8.3.3
colorama==0.4.6
fastapi==0.136.1
greenlet==3.5.0
h11==0.16.0
idna==3.13
pydantic==2.13.3
pydantic_core==2.46.3
SQLAlchemy==2.0.49
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.46.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
# IANA timezone database for zoneinfo. Required for LibreLinkUp timestamp->UTC
# conversion (app/services/libre_timestamp.py): without it, zoneinfo can't
# resolve names like "Asia/Kolkata" on systems lacking a system tz database
# (notably Windows) and silently falls back to UTC — reintroducing the exact
# naive-UTC mis-stamping this fix removes.
tzdata>=2024.1

# Loads backend/.env into the process environment (app/__init__.py) —
# nothing else did this, which is why JWT_SECRET etc. weren't visible to
# uvicorn even with the values sitting in .env.
python-dotenv>=1.0.1
