org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Gradle 8.14 doesn't support Java 25 (Android Studio's bundled JBR, the only
# JDK otherwise auto-detected on this machine) — pin to a JDK Gradle/AGP 8.11.1
# actually support. Forward slashes deliberately: Java .properties files treat
# backslash as an escape character, so a raw Windows path here would corrupt.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.20.8-hotspot
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
