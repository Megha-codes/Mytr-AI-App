import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Glucose range colours
  static const glucoseGreen      = Color(0xFF4CAF50);
  static const glucoseOrange     = Color(0xFFFF9800);
  static const glucoseRed        = Color(0xFFE53935);
  static const glucoseDarkRed    = Color(0xFFB71C1C);
  static const glucoseDarkOrange = Color(0xFFE65100);

  // New Brand Colors
  static const limeAccent        = Color(0xFF6DC534);
  static const electricIndigo    = Color(0xFF6366F1);
  static const vividOrange       = Color(0xFFF97316);
  static const cyan              = Color(0xFF00C2E0); // Bolus/AI features

  // Dark Aesthetics
  static const showcaseBackdrop  = Color(0xFF111117);
  static const nearBlack         = Color(0xFF111111); // Background (dark), Text primary
  static const cameraBackground  = Color(0xFF0D0D0D);
  static const darkSurface       = Color(0xFF1A1A22);

  // Light Aesthetics
  static const sageGreen         = Color(0xFFE4E9DE);
  static const cream             = Color(0xFFF8F3EC); // Background (light sections)
  static const surfaceWhite      = Color(0xFFFFFFFF); // Surface cards
  static const borderLight       = Color(0xFFDDD5C5); // Card Borders

  static const error             = Color(0xFFEF4444);

  // Text
  static const textSecondary     = Color(0xFFB0A898);
}
